[project]
name = "project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flax>=0.12.0",
    "jax[cuda12]>=0.8.0",
    "optax>=0.2.6",
    "plotly>=6.4.0",
]

[dependency-groups]
dev = [
    "ruff>=0.14.5",
]
